# Dockerfile для Next.js приложения (для будущего использования)
FROM node:20-alpine

# Установка зависимостей
RUN apk add --no-cache libc6-compat

# Создание рабочей директории
WORKDIR /app

# Копирование package файлов (создать когда будет Next.js проект)
# COPY web/package*.json ./
# RUN npm ci

# Копирование исходного кода
# COPY web/ ./

# Сборка приложения
# RUN npm run build

# Экспорт порта
EXPOSE 3000

# Запуск приложения
# CMD ["npm", "start"]

